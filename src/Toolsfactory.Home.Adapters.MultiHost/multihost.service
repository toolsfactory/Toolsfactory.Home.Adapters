[Unit]
Description=Multihost - Gasprices, Abfall and Weather for OpenHab
After=network.target

[Service]
ExecStart=/usr/share/ohmultihost/multihost.host
Restart=always
User=pi
# Note Debian/Ubuntu uses 'nogroup', RHEL/Fedora uses 'nobody'
Group=nogroup
WorkingDirectory=/usr/share/ohmultihost
Environment=PATH=/usr/bin:/usr/local/bin:/usr/share/dotnet:/opt/dotnet/shared:/opt/dotnet
Environment=MULTIHOST_CONF=/etc/ohadapters/multihost
Environment=DOTNET_ROOT=/opt/dotnet

[Install]
WantedBy=multi-user.target
